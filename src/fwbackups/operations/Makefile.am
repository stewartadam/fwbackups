PYTHON_FILES = \
    __init__.py \
    backup.py \
    restore.py

EXTRA_DIST = \
    $(PYTHON_FILES)

pydir = $(pkgdatadir)/fwbackups/operations
py_PYTHON = $(PYTHON_FILES)

clean-local:
	rm -f *.pyc *.pyo
